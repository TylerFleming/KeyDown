 import { styledButton } from './Button.style'

 
 const Button = ({text}) => {
     return (
         <styledButton bgColor="black" textColor="#fff">{text}</styledButton>
     )
 }
 
 export default Button
 